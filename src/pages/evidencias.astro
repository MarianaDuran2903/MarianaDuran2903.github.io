---
import Layout from "../layouts/Layout.astro";

const tabs = [
  { label: "Modelado", key: "modelado" },
  { label: "Normalización", key: "normalizacion" },
  { label: "SQL", key: "sql" },
  { label: "Proyecto", key: "proyecto" },
];
---

<Layout>
  <section class="min-h-screen bg-gray-950 px-6 pt-28">
    <div class="mx-auto max-w-6xl text-center">
      <h1 class="text-5xl font-bold text-white text-shadow">Evidencias</h1>
      <p class="mt-4 text-gray-300">
        Aquí iré subiendo mis evidencias del curso Bases de Datos 1 (por ahora está en construcción).
      </p>

      <!-- Submenú tipo “chips” -->
      <div class="mt-10 flex flex-wrap justify-center gap-4">
        {tabs.map((t, i) => (
          <button
            type="button"
            class:list={[
              "rounded-full px-6 py-2 font-semibold transition",
              i === 0
                ? "bg-primary text-white drop-shadow-[2px_2px_0_#0debd8]"
                : "bg-gray-800 text-gray-200 hover:bg-gray-700",
            ]}
          >
            {t.label}
          </button>
        ))}
      </div>

      <!-- Contenido vacío por ahora -->
      <div class="mt-12 rounded-2xl border border-white/10 bg-gray-900/40 p-8 text-left text-gray-300">
        <h2 class="text-2xl font-bold text-white">En construcción</h2>
        <p class="mt-2">
          Próximamente: documentos, capturas, enlaces y entregas organizadas por tema.
        </p>
      </div>
    </div>
  </section>
</Layout>